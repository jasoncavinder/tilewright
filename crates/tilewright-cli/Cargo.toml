[package]
name = "tilewright-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Command-line tooling powered by the Tilewright library."
publish = false

[[bin]]
name = "tilewright"
path = "src/main.rs"

[dependencies]
tilewright = { version = "0.1.0", path = "../tilewright" }
