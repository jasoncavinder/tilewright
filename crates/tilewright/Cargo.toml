[package]
name = "tilewright"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Core library for reading, validating, and editing tile-based RPG project data."
publish = false

[dependencies]
